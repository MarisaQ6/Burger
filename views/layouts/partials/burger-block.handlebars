<li>
	{{name}}

	<button class="change-yum" data-id="{{id}}" data-newyum="{{yum}}">
		{{#if yum}}Time for Burgers!{{else}}Yum!{{/if}}
	</button>

	<button class="delete-burgers" data-id="{{this.id}}">Nummy</button>
</li>
